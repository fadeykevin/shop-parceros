[build]
builder = "nixpacks"
buildCommand = "bash build.sh"

[deploy]
startCommand = "gunicorn config.wsgi:application --bind 0.0.0.0:8080"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
